# Connectia 서비스 아카이브 (PRODUCT.md)

> 이 문서는 Connectia가 어떤 서비스이고 각 화면·기능이 어떻게 구성되어 있는지를 기록하는 **단일 참조 문서**다.
> 목적: 작업 시 코드를 읽지 않아도 서비스와 해당 화면의 현재 상황을 파악할 수 있게 한다.
> 화면 파일 위치는 [SCREENS.md](SCREENS.md) 참조. **화면이 바뀌면 이 문서도 함께 갱신한다.**

---

## 1. 서비스 개요

**Connectia(커넥티아)** — 소상공인·전문직(병원, 공인중개사, 보험설계사, 개인 전문가 등)이 자신을 소개하는 **AI 프로필 페이지(명함)** 를 만들어 공유하는 서비스.

- 페이지 주인은 프로필·연락처·소개·갤러리·게시판·이벤트를 한 페이지에 담아 링크로 공유한다.
- 방문자는 페이지에서 정보를 보고, **AI 채팅**으로 질문하면 페이지 주인이 학습시킨 자료를 바탕으로 AI가 대신 답한다.
- 페이지 주인은 이벤트(랜딩+신청폼)로 고객 정보를 수집하고, 통계 화면에서 활동을 확인한다.
- 주 사용자는 **IT에 익숙하지 않은 소상공인** — 모든 화면은 "최대한 단순하게"가 원칙. 뭔가 작성하거나 수정하는 화면에는 **미리보기를 제공하거나, 실제 보여질 화면과 비슷한 예측 가능한 형태**를 차용한다.

---

## 2. 핵심 개념 (도메인 용어)

| 용어 | 정의 |
|---|---|
| **페이지 (AI 페이지 / AI 아바타)** | 서비스의 기본 단위. 한 사람·팀·매장의 프로필+AI 채팅+게시판+이벤트가 담긴 공유 가능한 페이지. 계정 하나가 여러 페이지를 소유할 수 있다 |
| **AI 학습 / 자료 채우기** | 방문자 질문에 AI가 답하도록 페이지 주인이 답변·자료를 채우는 행위. 프로필 편집의 각 입력에도 "AI 학습" 토글이 있음 |
| **미답변 질문** | AI가 답하지 못한 방문자 질문. 통계 화면의 최상단 히어로("이 N개만 채우면 AI가 답할 수 있어요")이자, 채팅 화면에서 인라인으로 답변 추가 가능 |
| **이벤트** | 고객 정보 수집용 랜딩 페이지 + 신청 폼. **제목만 채우면 발행 가능.** 신청 폼에는 이름·연락처·이메일이 기본으로 떠 있되 체크박스로 제외할 수 있고, 주관식·객관식 입력폼을 자유롭게 추가할 수 있다 |
| **이벤트 섹션 / 게시판 섹션** | 프로필 페이지에서 이벤트와 게시판은 별도 섹션으로 나뉘며, **이벤트 섹션은 배너 형태**로 노출된다 |
| **커뮤니티 (팀 페이지)** | 여러 멤버가 공동 운영하는 페이지. 운영자/멤버 권한 구분, 멤버 목록·초대 기능 포함 |
| **팀 단위 페이지 관리** | 초대 링크로 팀원들의 개인 페이지를 그룹으로 묶어 관리·모니터링하는 기능 (팀이 관리자로 참여) |

---

## 3. 사용자 유형과 주요 흐름

| 유형 | 설명 |
|---|---|
| **페이지 주인 (owner)** | 페이지를 만들고 편집·운영. 프로필 편집, AI 학습, 이벤트 발행, 통계 확인, 멤버 관리 |
| **방문자 (고객)** | 공유받은 페이지를 열람. AI 채팅 질문, 이벤트 신청, 연락처 저장 |
| **멤버** | 커뮤니티 페이지의 참여자. 자기 프로필 수정, 게시글·댓글 작성 (권한은 운영자가 부여) |

**주요 흐름:**
1. **온보딩**: 서비스 유형 선택 → 템플릿 미리보기 → 페이지 생성 → 로그인 → 온보딩 투어
2. **페이지 운영**: 프로필 편집(배너·연락처·About·갤러리·게시판 구성) → 링크 공유
3. **AI 운영 루프**: 방문자 질문 → 미답변 발생 → 통계/채팅에서 확인 → 답변 채움 → AI 학습
4. **이벤트 루프**: 이벤트 소개 → 입력폼 작성 → 발행(랜딩) → 방문자 신청 → 신청자 확인(엑셀 다운로드, AI 학습 연동)

---

## 4. 화면별 상세

> 각 화면의 확정본은 `screens/`에 있다. 아래 분류는 기능 도메인 기준.

### 4.1 온보딩·계정

#### 로그인 (`screens/login.html`)
- **목적**: 이메일·SNS로 로그인/가입하는 진입점. 지역별(한국/해외) 로그인 옵션 데모 포함.
- **구성**: 로고 → 소셜 로그인(한국: 카카오 우선) → 디바이더 → 보조 아이콘 버튼(구글·이메일) → 개인정보 처리방침. 서브 화면: 이메일 입력 / 비밀번호 / 회원가입 / 약관동의.
- **기능**: 로케일 토글(데모), 비밀번호 표시 토글, 약관 전체/개별 동의(미동의 시 가입 버튼 비활성), 이메일 신규/기존 분기.

#### 프로필 시작·온보딩 (`screens/profile-start.html`)
- **목적**: 신규 사용자의 페이지 생성 흐름. 서비스 유형 선택 → 템플릿 미리보기 → 페이지 생성 → 로그인 → 온보딩 투어.
- **구성**: 홈(서비스 유형 카드: 이력서/세일즈/서비스 소개서/프로필 페이지/CS 등) → 템플릿 프리뷰("이 템플릿으로 만들기") → 생성된 프로필 + 온보딩 투어.
- **기능**: 온보딩 투어(프로필편집·설정 버튼 하이라이트), 알림톡 수신 동의 토글. ※ 프로토타입에 있는 명함 스캔 흐름은 실제 서비스에 없음.

### 4.2 페이지 관리·편집

#### 프로필 편집 (`screens/profile-edit.html`)
- **목적**: 페이지의 모든 구성 요소를 편집하는 통합 편집 화면.
- **구성**(위→아래): 배너·프로필 사진(템플릿 선택, 커버 스타일: 겹침/아래/숨김, 자동채우기) → 기본정보(이름·핸들·프로필 주소) → 연락처(전화·이메일·주소) → SNS → About(리치텍스트) → 이벤트 목록(드래그 정렬) → 갤러리(가로/세로/격자 레이아웃) → 게시판(표시 개수 3/5/10) → 리뷰 미리보기 → 추가정보.
- **기능**: 섹션 이름 수정, 리치텍스트 에디터(굵게/기울임/밑줄/취소선+글자수), 프로필 주소 중복 확인(사용가능/불가), **모든 입력에 AI 학습 토글**, 하단 "적용" 저장.

#### 내 AI 아바타 (`screens/my-ai-avatars.html`)
- **목적**: 내가 만든 페이지들과 가입한 커뮤니티를 관리하는 바텀시트. 기본 페이지 설정.
- **구성**: 메인 뷰(탭: 내 AI 페이지 / 가입한 커뮤니티, 검색, 목록) + 하단 CTA "새 AI 아바타 만들기" / 설정 뷰(기본 페이지 라디오 선택).
- **기능**: 탭 전환, 실시간 검색 필터, 기본값 변경 감지 시 "완료" 버튼 표시. 커뮤니티 빈 상태("아직 가입한 커뮤니티가 없어요").

#### 팀 단위 페이지 관리 (`screens/team-page.html`)
- **목적**: 초대 링크로 팀원 페이지를 그룹으로 관리·모니터링.
- **구성**: 페이지 전환 시트(탭+필터 칩) → 관리 화면(초대 링크 카드: URL 복사, 활성/비활성 토글, 받은 페이지 수, 참여자 아바타 스택) → 초대받은 사용자용 링크 랜딩(팀 로고·설명·"시작하기").
- **기능**: 링크 생성/이름 수정/삭제(링크만 vs 관리자 권한까지 선택), 링크 복사, 링크별 페이지 필터링. 링크 0개 빈 상태.

### 4.3 페이지 (방문자 대면)

#### 게시판·보드 (`screens/board.html`)
- **목적**: 페이지의 메인 화면. 프로필 히어로 + 갤러리 + 게시판 + 이벤트를 통합한 랜딩.
- **구성**: 커버 배너 + 프로필(연락처 저장·공유 버튼, 대화하기 CTA) → 탭(홈/**갤러리**/이벤트·행사/게시판/리뷰) → 홈 탭(갤러리 섹션 전체 + **이벤트 섹션(배너 형태 노출)** + 게시판 섹션) → 갤러리 탭(모든 갤러리 섹션 통합).
- **갤러리**: 갤러리 섹션은 여러 개 추가 가능(프로필 편집). 방문자 화면에선 갤러리 N개 → **탭 1개로 통합**(섹션별 소제목 + 격자를 세로 스택). 섹션 레이아웃은 격자(인스타형 3열 4:5) 또는 **가로 캐러셀**(16:9, 사진 전용·동영상 없음). 홈 탭에도 동일 갤러리 섹션 전체를 노출하며, 섹션 사이는 디바이더로 구분. 각 섹션 헤더에 [+추가] 버튼.
- **기능**: 화면 전환(goTo), 글쓰기·이미지 첨부(드래그 정렬), 댓글/답글, 이벤트 신청 폼 커스터마이징, 게시글 공개/비공개, 멤버 관리.
- **분기**: 운영자 vs 멤버 시점(data-viewer), 이벤트 진행중/종료, 각종 빈 상태.

#### 커뮤니티 멤버 (`screens/community-member.html`)
- **목적**: 공동 운영 페이지의 멤버 관리 중심 화면. board와 같은 골격 + 멤버 탭 강화.
- **구성**: 랜딩(게시판·갤러리·About·리뷰) → 멤버 탭(목록, 멤버 상세 모달: 연락처·채팅/전화/이메일 액션) → 게시판/이벤트/초대 탭.
- **기능**: 운영자/멤버 시점 전환(CSS 분기), 운영자만 권한 드롭다운·연락처 열람, 멤버는 자기 프로필 수정 모달(소개글), 초대 링크 생성·이력.

#### 글쓰기 (`screens/post-write.html`)
- **목적**: 게시글·공지·이벤트 작성/편집 흐름.
- **구성**: 랜딩 → 글쓰기(제목·내용, 이미지 드래그앤드롭 첨부·정렬, 공지 체크박스, AI 요약 토글, 이벤트 설정 패널) → 게시글 상세(댓글) → 멤버·설정 화면.
- **기능**: 이벤트 유형 라디오·기간 입력, 신청 폼 필드 추가/삭제, 댓글 편집 모드, 글 수정/삭제 바텀시트.

#### 영업 프로필 (`screens/sales-profile-name.html`)
- **목적**: 매장·영업용(예: 엘린피부과) 프로필 랜딩. 고객의 연락처 저장·공유·예약 CTA 중심.
- **구성**: 프로필 헤더(연락처 저장·공유) → 매장명 → 연락처(전화·이메일·주소·SNS) → About(마크다운) → 예약 바로가기.

#### 이력서 프로필 (`screens/resume-profile.html`)
- **목적**: 개인 이력서형 페이지. 이력서 본문 + AI 채팅 패널(지원자 관련 질문 응답).
- **구성**: 좌: 이력서(프로필 헤더, 소개·스킬·학력·경력·경력기술서·활동·자격·포트폴리오) / 우: 고정 채팅 패널(추천 질문 칩, 입력창). 모바일에서는 채팅이 풀스크린 오버레이 + 플로팅 버튼.
- **기능**: 채팅 전송·타이핑 애니메이션, 추천 칩 자동 전송, 언어 드롭다운, 채팅 접기, PDF 저장·공유링크 복사. ※ 유일하게 tokens.css 미사용(독립 스타일).

### 4.4 AI 답변·통계

#### 답변 플로우·채팅 (`screens/answer-flow.html`)
- **목적**: 채팅 기록에서 미답변 질문을 발견하고 그 자리에서 답을 채워 AI를 학습시키는 흐름.
- **구성**: 채팅방(봇/방문자 메시지, 날짜 디바이더) → 미답변 질문의 "답변하기" CTA → 답변 바텀시트(질문 표시, 답 입력, 파일 첨부) → 확인 다이얼로그("AI가 답변을 학습했어요" + 방문자에게 보내기/보내지 않기).
- **기능**: CTA 클릭 시 해당 질문으로 스크롤+시트 오픈, 쿼리 파라미터(?q=)로 특정 질문 자동 오픈.

#### 통계 (`screens/statistics.html`)
- **목적**: 숫자 구경이 아니라 **"미답변 질문에 답을 채우게 만드는"** 행동 유도 화면. (상세: [통계 PRD](prototypes/statistics-0611/통계화면_개선_PRD.md))
- **구성**(위→아래=중요도): 기간 세그먼트(오늘/이번 주/이번 달) → 히어로 "채워야 할 질문"(많이 물어본 순+횟수, 답변율 보조 표시) → 핵심 지표 2개(받은 대화/페이지를 본 사람, 그래프 없이 숫자+한 줄 증감) → 정보 제출한 사람 → 자주 묻는 질문 → 최근 대화.
- **원칙**: 추세 그래프 금지, 모든 숫자는 선택 기간 기준 일괄 적용, 빈 상태는 긍정 문구("지금은 AI가 다 답하고 있어요"), 0은 부드럽게("오늘은 아직 받은 대화가 없어요").

### 4.5 이벤트

#### 이벤트 소개 (`screens/event-intro.html`)
- **목적**: 이벤트 기능이 뭔지 보여주는 온보딩 화면. 폰 목업 안에 완성된 랜딩 예시를 표시.
- **구성**: 상단 바 → 폰-인-폰 목업(랜딩 미리보기) → 하단(다시 보지 않기 체크, "이벤트 만들기" CTA).

#### 이벤트 입력폼 (`screens/event-input.html`)
- **목적**: 사업자가 한 화면에서 이벤트 랜딩을 채워 발행.
- **구성**: 대표 이미지 업로더(썸네일 스트립) → 이벤트 이름 → 신청 기간(달력 피커, 시간 토글, "신청 기간과 동일" 옵션) → 이벤트 설명 → 신청폼 구성 → 리뷰 노출(on/off 토글) → 신청 버튼 문구.
- **신청폼 구성**: 상단에서 **신청 방식 선택**(카드형 세그먼트, 한 줄 2개) — `자체 신청폼` / `외부 링크`.
  - **자체 신청폼(기본)**: 이름·연락처·이메일이 기본으로 떠 있되 **체크박스로 제외 가능**. 그 외 **주관식·객관식 질문을 자유롭게 추가**(질문 추가/삭제, 객관식 보기 관리, 복수 선택 허용, 항목별 필수 토글).
  - **외부 링크**: 구글폼 등 이미 쓰는 신청폼 URL을 연결. 기본 필드·질문 구성 UI는 숨겨지고, 발행 시 랜딩 신청 버튼이 이 링크로 이동. 신청자를 집계할 수 없어 이벤트 설정의 **"신청자 수 노출"이 자동 비활성화**된다.
- **원칙**: **제목(이벤트 이름)만 채우면 발행 가능.** 나머지는 선택. "미리보기" 버튼으로 고객이 보는 랜딩을 실시간 확인(sessionStorage로 event-landing에 전달 — 외부 링크 여부·URL 포함).
- ※ [이벤트 입력폼 PRD](prototypes/event-landing-0616/event_input_form_PRD_v2.md)는 구버전(v1 기준, 필수 4개 시절) — 현재 사양은 이 문서와 `screens/event-input.html`이 기준.

#### 이벤트 랜딩 (`screens/event-landing.html`)
- **목적**: 고객이 보는 이벤트 페이지. 정보 확인 → 신청 진입.
- **구성**: 히어로 슬라이더(1/N 카운터) → 병원명·제목·별점·후기 수 → 신청 기간 카드(D-day) → 설명(더보기 접힘) → 실제 후기 스크롤 → 병원 페이지 링크 카드 → sticky CTA(신청자 수 + 신청 버튼).
- **분기**: 설명 짧으면 더보기 숨김, 후기 비활성 시 별점·후기 숨김. **외부 링크 이벤트**면 sticky CTA가 외부 폼을 새 탭으로 열고(외부 이동 아이콘 병기) 신청자 수 메타를 숨긴다.

#### 이벤트 신청자 확인 (`screens/event-applicants-check.html`)
- **목적**: 이벤트 소유자가 신청자 목록을 확인·관리.
- **구성**: 이벤트 카드(수집 정보 요약) → 총 신청 수 + 엑셀 다운로드 → 신청자 행(이름, 일시, 필드별 답변 — 텍스트/선택지 칩) → 더보기 바텀시트(AI 학습 토글, 수정, 이벤트 종료, 삭제).
- **기능**: CSV 내보내기, 빈 필드 "—" 표시, 복수 선택 답변은 칩 배열.

---

## 5. 참고 문서

| 문서 | 내용 |
|---|---|
| [SCREENS.md](SCREENS.md) | 화면 인덱스 — 확정본↔원본 매핑, 폴더 구조, 승격 흐름 |
| [CLAUDE.md](CLAUDE.md) | 디자인 시스템 규칙 (토큰·컴포넌트·여백) |
| [통계 PRD](prototypes/statistics-0611/통계화면_개선_PRD.md) | 통계 화면의 상세 기획 원칙 |
| [이벤트 입력폼 PRD](prototypes/event-landing-0616/event_input_form_PRD_v2.md) | 이벤트 만들기 입력창 사양 — **구버전** (필수 4개 시절, 현재는 제목만 필수) |
| [프로필 배너 PRD](prototypes/board-profile-0625/profile_banner_prd.md) | 배너 기능 기획 — **구버전** (현재는 이벤트 섹션을 배너 형태로 노출하는 방식으로 변경) |
| DESIGN-toss.md / DESIGN-toss-2.md | 토스 디자인 시스템 분석 (디자인 참고 자료) |

## 6. 유지 규칙

1. 화면이 `screens/`에 새로 반영(승격)되면 **이 문서의 해당 화면 섹션도 같은 작업에서 갱신**한다.
2. 새 화면이 추가되면 4장에 항목을 추가하고, 도메인 용어가 생기면 2장에 등록한다.
3. 제품 방향이 바뀌면 1장의 "제품 방향"을 갱신한다 (날짜 명기).
4. 기획 문서(PRD)가 새로 생기면 5장 표에 추가한다.
